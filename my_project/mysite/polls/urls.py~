### ADD THIS FILE IN 

from django.conf.urls import url
from . import views
#### beginning ####
##urlpatterns = [url(r'^$', views.index, name='index')]

## DO
urlpatterns = [
    url(r'^polls/', include('polls.urls')), url
    url(r'^admin/', admin.site.urls),
]

## include allows referencing other URLconfs
